version: '3.4'
services:
  workflow-test.avado.dappnode.eth:
    build: ./build
    image: 'workflow-test.avado.dappnode.eth:0.0.1'
    restart: always
    volumes:
      - 'data:/data'
volumes:
  data: {}
